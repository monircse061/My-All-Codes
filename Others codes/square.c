#include<stdio.h>
int main()
{
    int n,x,a,b,j,i;
    while(scanf("%d%d",&a,&b)&&(a,b)!=0)
    { j=0;for(i=a;i<=b;i++)
        {x=sqrt(i);
    if(i==x*x)
     j++;}

    printf("%d\n",j);}
    return 0;

}
