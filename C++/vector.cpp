#include<bits/stdc++.h>
using namespace std;
int main(){
   /**vector<int>v1;
   vector<int>::iterator l;
   vector<int>::reverse_iterator l1;

   for(int i=0;i<10;i++){
    v1.push_back(i);
   }
   //for(int i=0;i!=v.size();i++){
    //cout<<v[i]<<" ";

   //}
    l=v1.begin();
    v1.erase(l+5);
   v1.insert(l+5,30);
  for(l=v1.begin();l!=v1.end();l++)
    cout<<*l<<" ";
    cout<<endl;
  for(l1=v1.rbegin();l1!=v1.rend();l1++){
    cout<<*l1<<endl;
  }**/
  char a[0];

 //for(int i=0;i<6;i++)
    {scanf("%s",a);
    printf("%s",a);
    getchar();
    scanf("%s",a);
    printf("%s",a);
    getchar();
    scanf("%s",a);
    printf("%s",a);
    getchar();
    scanf("%s",a);
    printf("%s",a);
  }
  //for(int i=0;i<6;i++)

  //a=min(a,a+5);
  //cout<<a;
   return 0;
}
