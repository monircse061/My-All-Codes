#include<bits/stdc++.h>
#include<string.h>
using namespace std;
int main(){
   char a[10005];int i,j=5,n,y,x,sum=0,sum1=0;
   /**int main(){
   long long a[10000],i,j,x=0,y,m,n;
   scanf("%I64d %I64d",&m,&n);
   while(m>0&&n!=10){

   }
   for(i=1;i<=m;i++){
    x=x+log10(i);
   }
}
  **/
   /**scanf("%d",&n);
   for(i=0;i<n;i++){
    scanf("%d",&a[i]);
   }
   for(i=0;a[j]!=0;i=i+2){
    if(a[i+2]>a[i+3])
        {x=a[i+2];}
    else
        {x=a[i+3];}
    sum=sum+x;
    if(a[i+3]>a[i+4])
        {y=a[i+3];}
    else
        {y=a[i+4];}
     sum1=sum1+y;
     j=i+4;
   }
   if(sum+a[0]>sum1+a[1])
    printf("%d\n",sum+a[0]);
   else
    printf("%d\n",sum1+a[1]);**/
    printf("%lf",cos(-1));
   return 0;
}
